const baseUrl = 'https://api.pokemontcg.io/v2/'

export const pokemonApi = {
  cards: `${baseUrl}cards`,
  sets: `${baseUrl}sets`,
  types: `${baseUrl}types`,
  subtypes: `${baseUrl}subtypes`,
  supertypes: `${baseUrl}supertypes`,
  rarities: `${baseUrl}rarities`,
}